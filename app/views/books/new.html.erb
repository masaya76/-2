<h1>User info</h1>
<%= form_with model: @book, local:true do |f| %>
  <h4>Name</h4>
  <%= f.attachment_field :title %>
  <h4>ショップ名</h4>
  <%= f.text_field :shop_name %>
  <h4>説明</h4>
  <%= f.text_area :caption %>
  <%= f.submit '投稿' %>
<% end %>